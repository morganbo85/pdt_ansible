---
- hosts: all
  become: true
  gather_facts: no
  tasks:

  - name: install packages
    package:
      name:
      - software-properties-common
      - curl
      - apt-transport-https
      - ca-certificates
      - gnupg


  - name: additional repositories for PHP
    ansible.builtin.apt_repository
      repo: ppa:ondrej/php
